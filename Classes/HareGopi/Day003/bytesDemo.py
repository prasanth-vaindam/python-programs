s = b"ABC"

# bytes is a data type used to store binary data as a sequence of bytes (0–255).
# It is immutable, meaning its contents cannot be changed after creation.

# String (str) → Stores text ("Hello")
# Bytes (bytes) → Stores the computer's binary representation of that text (b'Hello')

# Think of a string as human-readable text and
# bytes as the computer-readable form of that text.


print(s[0])
