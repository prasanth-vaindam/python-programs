# read length and breadth values from user

length = float(input("Enter length of rectangle: "))
breadth = float(input("Enter breadth of rectangle: "))

Area = length * breadth
print("Area of the rectangle is : ", Area)